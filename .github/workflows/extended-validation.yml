name: Extended Validation

on:
  push:
    branches: [main]
  schedule:
    - cron: '0 7 * * *'
  workflow_dispatch:

concurrency:
  group: extended-validation-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name != 'workflow_dispatch' }}

permissions:
  contents: read

jobs:
  trusted:
    uses: OMT-Global/mailplus-intelligence/.github/workflows/extended-trusted.yml@d08b5ee74df3eb6d016e9e4aa267acc4bcafd154 # v1
