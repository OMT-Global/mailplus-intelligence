#!/usr/bin/env bash
set -euo pipefail

echo "No repo-specific artifact publish step is configured."
echo "Create exact release tags such as v1.2.3; the reusable release workflow handles GitHub release publication and floating tag promotion."
